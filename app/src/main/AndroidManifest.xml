<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="maustemies.passprotecter">

    <application
        android:allowBackup="true"
        android:icon="@drawable/keys_of_st_peter_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateFileActivity" />
        <activity android:name=".OpenFileActivity"></activity>
        <activity android:name=".PasswordQueryPopUp" android:theme="@style/AppTheme.PasswordPopUp"></activity>
        <activity android:name=".PasswordAddPopUp" android:theme="@style/AppTheme.PasswordPopUp"></activity>
    </application>

</manifest>